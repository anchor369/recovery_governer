PGHOST=localhost
PGPORT=5432
PGDATABASE=razorpay_recovery
PGUSER=postgres
PGPASSWORD=your_postgres_password

# Optional bounded connection-pool settings.
PGPOOL_MIN_SIZE=1
PGPOOL_MAX_SIZE=5
PGPOOL_TIMEOUT=30
